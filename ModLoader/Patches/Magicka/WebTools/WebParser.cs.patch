@@ -23,3 +23,7 @@
-		{
-			Action<bool> action = (Action<bool>)iArguments;
-			try
+        {
+            if (iResponse == null)
+                return;
+
+			var action = (Action<bool>)iArguments;
+
+            try
